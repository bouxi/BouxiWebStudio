# see https://symfony.com/doc/current/reference/configuration/framework.html
framework:
    secret: '%env(APP_SECRET)%'

    # Démarrage de session automatique si on lit/écrit dedans
    session: true

    # 🔹 Active le composant Form
    form: true

    # 🔹 Active la protection CSRF (utile pour les formulaires)
    csrf_protection: true

    # 🔹 Active le système de validation (contraintes @Assert, etc.)
    validation:
        enabled: true

when@test:
    framework:
        test: true
        session:
            storage_factory_id: session.storage.factory.mock_file
