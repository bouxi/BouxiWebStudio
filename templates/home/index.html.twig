{% extends 'base.html.twig' %}

{% block title %}Accueil — BouxiWebStudio{% endblock %}

{% block body %}
    <div class="text-center mt-5">
        <h1 class="mb-3">Bienvenue sur BouxiWebStudio</h1>
        <p class="text-secondary mb-4">
            Environnement Symfony prêt. Authentification en place. On peut construire le site. 🚀
        </p>

        {% if not app.user %}
            <a href="{{ path('app_register') }}" class="btn btn-success me-2">
                Créer un compte
            </a>
            <a href="{{ path('app_login') }}" class="btn btn-outline-light">
                Se connecter
            </a>
        {% else %}
            <p class="mt-3">
                Connecté en tant que <strong>{{ app.user.fullname ?? app.user.email }}</strong>.
            </p>
        {% endif %}
    </div>
{% endblock %}
